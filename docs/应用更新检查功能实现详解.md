# 【Electron】应用更新检查功能：实现自动更新机制

> **作者**: HarmonyPC Electron Team  
> **发布日期**: 2026-03-04  
> **难度**: ⭐⭐ 中等  
> **阅读时间**: 约 25 分钟  
> **相关代码**: `app/updater.js`

---

## 📖 引言

应用自动更新是桌面应用的重要功能，它允许用户无缝获取最新版本，无需手动下载和安装。对于 Electron 应用来说，实现自动更新不仅能提升用户体验，还能确保用户始终使用最新、最安全的版本。

### 自动更新的应用场景

- **企业级应用**：需要及时推送功能更新和安全补丁
- **开发者工具**：频繁更新以修复 bug 和添加新功能
- **生产力工具**：持续改进以提升用户效率
- **商业软件**：版本管理和授权控制

### 本文将实现的功能

✅ 自动检测应用更新  
✅ 版本号比较和判断  
✅ 下载更新包并显示进度  
✅ 安装更新包  
✅ 更新通知和提示  
✅ 鸿蒙平台适配  
✅ 更新历史记录  

---

## 🔧 技术方案

### 自动更新核心概念

#### 1. 版本号规范

遵循语义化版本规范（Semantic Versioning）：

```
主版本号.次版本号.修订号
例如: 1.0.0, 2.3.1, 3.0.0-beta.1
```

| 部分 | 说明 | 示例 |
|------|------|------|
| 主版本号 | 不兼容的 API 修改 | 1.0.0 → 2.0.0 |
| 次版本号 | 向下兼容的功能性新增 | 1.0.0 → 1.1.0 |
| 修订号 | 向下兼容的问题修正 | 1.0.0 → 1.0.1 |

#### 2. 更新流程

```
┌─────────────┐
│  应用启动   │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ 检查更新    │
└──────┬──────┘
       │
       ▼
┌─────────────┐    是    ┌─────────────┐
│ 有新版本？  │────────▶│ 下载更新包  │
└──────┬──────┘          └──────┬──────┘
       │ 否                      │
       ▼                         ▼
┌─────────────┐          ┌─────────────┐
│  继续运行   │          │ 安装更新包  │
└─────────────┘          └──────┬──────┘
                                │
                                ▼
                         ┌─────────────┐
                         │ 重启应用    │
                         └─────────────┘
```

### 架构设计

```
┌─────────────────────────────────────┐
│         Main Process (主进程)         │
│                                     │
│  ┌─────────────────────────────┐   │
│  │    updater.js (更新模块)     │   │
│  │  - initUpdater()            │   │
│  │  - checkForUpdates()        │   │
│  │  - downloadUpdate()         │   │
│  │  - installUpdate()          │   │
│  │  - quitAndInstall()         │   │
│  └─────────────────────────────┘   │
│                                     │
│  ┌─────────────────────────────┐   │
│  │   Update Server (更新服务器)  │   │
│  │  - GitHub Releases         │   │
│  │  - 自定义服务器             │   │
│  └─────────────────────────────┘   │
│                                     │
│  ┌─────────────────────────────┐   │
│  │   main.js (主进程入口)       │   │
│  │  - initUpdater()            │   │
│  │  - IPC 通信                 │   │
│  │  - 菜单集成                 │   │
│  └─────────────────────────────┘   │
└─────────────────────────────────────┘
```

---

## 💻 代码实现

### 1. 创建更新模块

创建 `app/updater.js` 文件，实现完整的更新功能：

```javascript
const { app, dialog, BrowserWindow, ipcMain } = require('electron');
const https = require('https');
const http = require('http');
const fs = require('fs');
const path = require('path');

// 更新配置
const UPDATE_CONFIG = {
    // 更新服务器地址
    updateServer: 'https://api.github.com/repos/jianguoxu/harmonypc-electron/releases/latest',
    // 下载服务器地址
    downloadServer: 'https://github.com/jianguoxu/harmonypc-electron/releases/download',
    // 更新检查间隔
    checkInterval: 24 * 60 * 60 * 1000, // 24小时
    // 自动下载更新
    autoDownload: false,
    // 自动安装更新
    autoInstall: false
};

// 当前版本
let currentVersion = null;

// 最新版本信息
let latestVersionInfo = null;

// 更新状态
let updateStatus = {
    checking: false,
    available: false,
    downloading: false,
    installing: false,
    error: null
};

// 下载进度
let downloadProgress = {
    percent: 0,
    transferred: 0,
    total: 0,
    speed: 0
};
```

### 2. 初始化更新模块

```javascript
/**
 * 初始化更新模块
 */
function initUpdater() {
    // 获取当前版本
    currentVersion = getCurrentVersion();

    console.log('更新模块初始化完成，当前版本:', currentVersion);

    // 如果不是开发模式，启动定时检查更新
    if (!app.isPackaged) {
        console.log('开发模式，跳过自动更新检查');
        return;
    }

    // 启动定时检查
    startAutoCheck();

    // 应用启动时检查一次更新
    setTimeout(() => {
        checkForUpdates(false);
    }, 5000); // 5秒后检查
}

/**
 * 获取当前版本
 */
function getCurrentVersion() {
    try {
        // 方法1: 从 package.json 读取
        const packagePath = path.join(__dirname, 'package.json');
        if (fs.existsSync(packagePath)) {
            const packageData = JSON.parse(fs.readFileSync(packagePath, 'utf-8'));
            return packageData.version || '0.0.0';
        }
    } catch (error) {
        console.warn('无法从 package.json 读取版本:', error);
    }

    // 方法2: 使用 app.getVersion()
    try {
        return app.getVersion();
    } catch (error) {
        console.warn('无法获取应用版本:', error);
    }

    return '0.0.0';
}
```

### 3. 检查更新

```javascript
/**
 * 检查更新
 * @param {boolean} showNotification 是否显示通知
 * @returns {Promise<Object>} 更新信息
 */
function checkForUpdates(showNotification = true) {
    return new Promise((resolve, reject) => {
        if (updateStatus.checking) {
            console.log('正在检查更新，跳过');
            resolve({ checking: true });
            return;
        }

        updateStatus.checking = true;
        updateStatus.error = null;

        console.log('开始检查更新...');

        // 请求更新服务器
        const options = {
            headers: {
                'User-Agent': 'HarmonyOS-Electron-Updater'
            }
        };

        https.get(UPDATE_CONFIG.updateServer, options, (response) => {
            let data = '';

            response.on('data', (chunk) => {
                data += chunk;
            });

            response.on('end', () => {
                try {
                    const releaseInfo = JSON.parse(data);
                    latestVersionInfo = releaseInfo;

                    const latestVersion = releaseInfo.tag_name.replace('v', '');
                    const hasUpdate = compareVersions(currentVersion, latestVersion) < 0;

                    updateStatus.checking = false;
                    updateStatus.available = hasUpdate;

                    console.log('当前版本:', currentVersion);
                    console.log('最新版本:', latestVersion);
                    console.log('有更新:', hasUpdate);

                    if (hasUpdate) {
                        console.log('发现新版本:', latestVersion);

                        // 通知主窗口
                        notifyUpdateAvailable(releaseInfo, showNotification);

                        // 记录更新日志
                        logUpdateCheck(currentVersion, latestVersion, true);
                    } else {
                        console.log('已是最新版本');
                        logUpdateCheck(currentVersion, latestVersion, false);

                        if (showNotification) {
                            showNotificationMessage('已是最新版本', `当前版本 ${currentVersion} 已是最新版本`);
                        }
                    }

                    resolve({
                        hasUpdate,
                        currentVersion,
                        latestVersion,
                        releaseInfo
                    });
                } catch (error) {
                    console.error('解析更新信息失败:', error);
                    updateStatus.checking = false;
                    updateStatus.error = error.message;
                    reject(error);
                }
            });
        }).on('error', (error) => {
            console.error('检查更新失败:', error);
            updateStatus.checking = false;
            updateStatus.error = error.message;
            reject(error);
        });
    });
}
```

### 4. 版本号比较

```javascript
/**
 * 比较版本号
 * @param {string} version1 版本1
 * @param {string} version2 版本2
 * @returns {number} -1: version1 < version2, 0: version1 == version2, 1: version1 > version2
 */
function compareVersions(version1, version2) {
    const v1 = version1.split('.').map(Number);
    const v2 = version2.split('.').map(Number);

    for (let i = 0; i < Math.max(v1.length, v2.length); i++) {
        const num1 = v1[i] || 0;
        const num2 = v2[i] || 0;

        if (num1 < num2) return -1;
        if (num1 > num2) return 1;
    }

    return 0;
}
```

### 5. 下载更新

```javascript
/**
 * 下载更新
 * @param {string} version 版本号
 * @param {Function} onProgress 进度回调
 * @returns {Promise<string>} 下载的文件路径
 */
function downloadUpdate(version, onProgress) {
    return new Promise((resolve, reject) => {
        const downloadUrl = getDownloadUrl(version);
        const fileName = `update-v${version}.zip`;
        const filePath = path.join(app.getPath('temp'), fileName);

        console.log('开始下载更新:', downloadUrl);

        updateStatus.downloading = true;
        updateStatus.error = null;

        // 重置进度
        downloadProgress = {
            percent: 0,
            transferred: 0,
            total: 0,
            speed: 0
        };

        const file = fs.createWriteStream(filePath);
        const url = new URL(downloadUrl);

        const protocol = url.protocol === 'https:' ? https : http;

        const request = protocol.get(downloadUrl, (response) => {
            if (response.statusCode !== 200) {
                reject(new Error(`下载失败: ${response.statusCode}`));
                return;
            }

            const totalSize = parseInt(response.headers['content-length'], 10);
            downloadProgress.total = totalSize;

            let startTime = Date.now();
            let lastTransferred = 0;

            response.on('data', (chunk) => {
                downloadProgress.transferred += chunk.length;
                downloadProgress.percent = (downloadProgress.transferred / totalSize) * 100;

                // 计算下载速度
                const elapsed = (Date.now() - startTime) / 1000;
                if (elapsed > 0) {
                    downloadProgress.speed = (downloadProgress.transferred - lastTransferred) / elapsed;
                    lastTransferred = downloadProgress.transferred;
                }

                // 通知进度
                if (onProgress) {
                    onProgress(downloadProgress);
                }

                // 通知主窗口
                if (global.mainWindow) {
                    global.mainWindow.webContents.send('download-progress', downloadProgress);
                }
            });

            response.pipe(file);

            file.on('finish', () => {
                file.close();
                updateStatus.downloading = false;

                console.log('下载完成:', filePath);

                // 验证文件完整性
                verifyUpdateFile(filePath)
                    .then(() => {
                        resolve(filePath);
                    })
                    .catch(error => {
                        reject(error);
                    });
            });
        });

        request.on('error', (error) => {
            fs.unlink(filePath, () => {});
            updateStatus.downloading = false;
            updateStatus.error = error.message;
            reject(error);
        });
    });
}
```

### 6. 安装更新

```javascript
/**
 * 安装更新
 * @param {string} filePath 更新文件路径
 * @returns {Promise<boolean>} 安装结果
 */
function installUpdate(filePath) {
    return new Promise((resolve, reject) => {
        console.log('开始安装更新:', filePath);

        updateStatus.installing = true;
        updateStatus.error = null;

        // 鸿蒙平台特殊处理
        if (process.platform === 'harmonyos') {
            installHarmonyOSUpdate(filePath)
                .then(() => {
                    updateStatus.installing = false;
                    resolve(true);
                })
                .catch(error => {
                    updateStatus.installing = false;
                    updateStatus.error = error.message;
                    reject(error);
                });
        } else {
            // 其他平台使用通用安装方法
            installGenericUpdate(filePath)
                .then(() => {
                    updateStatus.installing = false;
                    resolve(true);
                })
                .catch(error => {
                    updateStatus.installing = false;
                    updateStatus.error = error.message;
                    reject(error);
                });
        }
    });
}

/**
 * 安装鸿蒙平台更新
 */
function installHarmonyOSUpdate(filePath) {
    return new Promise((resolve, reject) => {
        try {
            // 鸿蒙平台需要使用特殊的安装方法
            console.log('鸿蒙平台更新安装（模拟）');

            // 模拟安装成功
            setTimeout(() => {
                console.log('鸿蒙平台更新安装完成');
                resolve(true);
            }, 2000);
        } catch (error) {
            reject(error);
        }
    });
}
```

### 7. 在主进程中集成更新

在 `main.js` 中集成更新功能：

```javascript
const updater = require('./updater');

// 初始化更新模块
updater.initUpdater();

// IPC 通信
ipcMain.handle('check-for-updates', async () => {
    return await updater.checkForUpdates(true);
});

ipcMain.handle('download-update', async (event, version) => {
    return await updater.downloadUpdate(version);
});

ipcMain.handle('install-update', async (event, filePath) => {
    return await updater.installUpdate(filePath);
});

ipcMain.handle('quit-and-install', () => {
    updater.quitAndInstall();
});

ipcMain.handle('get-update-status', () => {
    return updater.getUpdateStatus();
});

// 创建菜单
function createMenu() {
    const template = [
        {
            label: '文件',
            submenu: [
                {
                    label: '检查更新',
                    click: () => {
                        updater.checkForUpdates(true);
                    }
                }
            ]
        }
    ];

    const menu = Menu.buildFromTemplate(template);
    Menu.setApplicationMenu(menu);
}
```

### 8. 在渲染进程中使用更新

在渲染进程中处理更新：

```javascript
// 检查更新
async function checkUpdates() {
    try {
        const result = await ipcRenderer.invoke('check-for-updates');
        if (result.hasUpdate) {
            showUpdateDialog(result);
        } else {
            showMessage('已是最新版本');
        }
    } catch (error) {
        showError('检查更新失败: ' + error.message);
    }
}

// 下载更新
async function downloadUpdate(version) {
    try {
        const filePath = await ipcRenderer.invoke('download-update', version);
        return filePath;
    } catch (error) {
        showError('下载更新失败: ' + error.message);
    }
}

// 监听下载进度
ipcRenderer.on('download-progress', (event, progress) => {
    updateProgressBar(progress.percent);
    updateDownloadSpeed(progress.speed);
});

// 监听更新可用
ipcRenderer.on('update-available', (event, info) => {
    showUpdateNotification(info);
});
```

---

## 🌏 鸿蒙平台适配

### 鸿蒙平台的特殊注意事项

#### 1. 更新包格式

鸿蒙平台可能需要特定的更新包格式：

```javascript
/**
 * 获取下载 URL（鸿蒙平台适配）
 */
function getDownloadUrl(version) {
    let platform = process.platform;

    // 平台映射
    if (platform === 'darwin') platform = 'macos';
    if (platform === 'win32') platform = 'windows';
    if (platform === 'linux') platform = 'linux';

    // 鸿蒙平台特殊处理
    if (process.platform === 'harmonyos') {
        platform = 'harmonyos';
        // 鸿蒙平台使用 .hap 格式
        return `${UPDATE_CONFIG.downloadServer}/v${version}/app-v${version}.hap`;
    }

    return `${UPDATE_CONFIG.downloadServer}/v${version}/harmonypc-electron-${platform}.zip`;
}
```

#### 2. 安装权限

鸿蒙平台可能需要特殊的安装权限：

```javascript
/**
 * 检查安装权限
 */
function checkInstallPermission() {
    if (process.platform !== 'harmonyos') {
        return Promise.resolve(true);
    }

    return new Promise((resolve) => {
        // 鸿蒙平台需要检查安装权限
        // 这里需要调用鸿蒙系统的权限 API
        console.log('检查鸿蒙平台安装权限');
        resolve(true);
    });
}
```

#### 3. 重启机制

鸿蒙平台的重启机制可能不同：

```javascript
/**
 * 重启应用（鸿蒙平台适配）
 */
function quitAndInstall() {
    console.log('准备重启应用以应用更新');

    if (process.platform === 'harmonyos') {
        // 鸿蒙平台特殊处理
        try {
            // 调用鸿蒙系统的重启 API
            console.log('鸿蒙平台重启（模拟）');
            app.quit();
        } catch (error) {
            console.error('鸿蒙平台重启失败:', error);
        }
    } else {
        // 其他平台
        app.quit();
    }
}
```

---

## 🎯 最佳实践

### 1. 版本号管理

```javascript
// ✅ 正确：遵循语义化版本
"version": "1.0.0"
"version": "1.1.0"
"version": "2.0.0"

// ❌ 错误：不规范的版本号
"version": "1.0"
"version": "v1.0.0"
"version": "1.0.0-beta"
```

### 2. 更新检查频率

```javascript
// ✅ 正确：合理的检查间隔
const checkInterval = 24 * 60 * 60 * 1000; // 24小时

// ❌ 错误：过于频繁的检查
const checkInterval = 60 * 1000; // 1分钟
```

### 3. 错误处理

```javascript
// ✅ 正确：完善的错误处理
try {
    const result = await checkForUpdates();
    // 处理结果
} catch (error) {
    console.error('检查更新失败:', error);
    // 显示用户友好的错误信息
    showError('检查更新失败，请稍后重试');
}

// ❌ 错误：没有错误处理
const result = await checkForUpdates();
// 如果失败，应用可能崩溃
```

### 4. 用户体验

```javascript
// ✅ 正确：提供清晰的用户反馈
showProgress(50); // 显示下载进度
showMessage('下载中... 50%');

// ❌ 错误：没有用户反馈
downloadUpdate(version); // 用户不知道发生了什么
```

---

## 🚀 扩展功能

### 1. 增量更新

```javascript
/**
 * 检查增量更新
 */
function checkIncrementalUpdate() {
    // 只下载变更的文件，而不是整个应用包
    const currentVersion = getCurrentVersion();
    const latestVersion = latestVersionInfo.tag_name;

    // 获取文件变更列表
    const diffFiles = getDiffFiles(currentVersion, latestVersion);

    // 只下载变更的文件
    diffFiles.forEach(file => {
        downloadFile(file);
    });
}
```

### 2. 回滚机制

```javascript
/**
 * 创建备份
 */
function createBackup() {
    const backupPath = path.join(app.getPath('temp'), 'backup');
    fs.copySync(app.getAppPath(), backupPath);
}

/**
 * 回滚更新
 */
function rollbackUpdate() {
    const backupPath = path.join(app.getPath('temp'), 'backup');
    fs.copySync(backupPath, app.getAppPath());
}
```

### 3. 更新策略配置

```javascript
/**
 * 配置更新策略
 */
const UPDATE_STRATEGIES = {
    // 立即更新
    immediate: {
        autoDownload: true,
        autoInstall: true,
        notifyUser: false
    },
    // 提示用户
    prompt: {
        autoDownload: false,
        autoInstall: false,
        notifyUser: true
    },
    // 静默下载
    silent: {
        autoDownload: true,
        autoInstall: false,
        notifyUser: false
    }
};

function setUpdateStrategy(strategy) {
    Object.assign(UPDATE_CONFIG, UPDATE_STRATEGIES[strategy]);
}
```

---

## 📊 性能优化

### 1. 更新检查优化

```javascript
// ✅ 优化：使用缓存
const updateCache = {
    lastCheck: 0,
    result: null
};

function checkForUpdates(force = false) {
    const now = Date.now();
    const cacheValid = now - updateCache.lastCheck < 60 * 60 * 1000; // 1小时

    if (!force && cacheValid) {
        return Promise.resolve(updateCache.result);
    }

    // 执行实际的检查
    return performUpdateCheck();
}
```

### 2. 下载优化

```javascript
// ✅ 优化：断点续传
function downloadUpdate(version, onProgress) {
    const filePath = getDownloadFilePath(version);
    const downloadedSize = getDownloadedSize(filePath);

    const options = {
        headers: {
            'Range': `bytes=${downloadedSize}-`
        }
    };

    // 从断点继续下载
    return downloadWithResume(downloadUrl, filePath, options, onProgress);
}
```

---

## 🐛 常见问题与解决方案

### 问题 1：版本号比较错误

**原因**：
- 版本号格式不规范
- 比较逻辑有误

**解决方案**：
```javascript
// 使用更健壮的比较函数
function compareVersions(version1, version2) {
    // 清理版本号
    const cleanVersion = (v) => v.replace(/^v/, '').trim();
    const v1 = cleanVersion(version1).split('.').map(Number);
    const v2 = cleanVersion(version2).split('.').map(Number);

    // 比较
    for (let i = 0; i < Math.max(v1.length, v2.length); i++) {
        const num1 = v1[i] || 0;
        const num2 = v2[i] || 0;

        if (isNaN(num1) || isNaN(num2)) {
            throw new Error('无效的版本号');
        }

        if (num1 < num2) return -1;
        if (num1 > num2) return 1;
    }

    return 0;
}
```

### 问题 2：下载失败

**原因**：
- 网络连接问题
- 下载 URL 错误
- 磁盘空间不足

**解决方案**：
```javascript
// 添加重试机制
async function downloadWithRetry(url, filePath, maxRetries = 3) {
    for (let i = 0; i < maxRetries; i++) {
        try {
            return await downloadUpdate(url, filePath);
        } catch (error) {
            console.error(`下载失败，重试 ${i + 1}/${maxRetries}:`, error);

            if (i === maxRetries - 1) {
                throw error;
            }

            // 等待后重试
            await new Promise(resolve => setTimeout(resolve, 2000 * (i + 1)));
        }
    }
}
```

### 问题 3：鸿蒙平台安装失败

**原因**：
- 权限不足
- 安装包格式不兼容
- 系统版本不匹配

**解决方案**：
```javascript
// 添加详细的错误日志
function installHarmonyOSUpdate(filePath) {
    return new Promise((resolve, reject) => {
        try {
            // 检查权限
            const hasPermission = checkInstallPermission();
            if (!hasPermission) {
                throw new Error('缺少安装权限');
            }

            // 检查系统版本
            const systemVersion = getHarmonyOSVersion();
            const requiredVersion = getRequiredVersion();
            if (compareVersions(systemVersion, requiredVersion) < 0) {
                throw new Error(`系统版本过低，需要 ${requiredVersion} 或更高版本`);
            }

            // 执行安装
            performInstallation(filePath)
                .then(resolve)
                .catch(reject);

        } catch (error) {
            console.error('鸿蒙平台安装失败:', error);
            reject(error);
        }
    });
}
```

---

## 📝 总结

本文详细介绍了如何在 Electron 应用中实现完整的自动更新功能，包括：

### 核心要点

1. **版本管理** - 遵循语义化版本规范，准确比较版本号
2. **更新检查** - 定时检查更新，支持手动触发
3. **下载管理** - 显示下载进度，支持断点续传
4. **安装流程** - 处理不同平台的安装差异
5. **平台适配** - 鸿蒙平台的特殊处理
6. **用户体验** - 清晰的提示和反馈

### 关键代码

```javascript
// 初始化更新模块
updater.initUpdater();

// 检查更新
const result = await updater.checkForUpdates(true);

// 下载更新
const filePath = await updater.downloadUpdate(version);

// 安装更新
await updater.installUpdate(filePath);

// 重启应用
updater.quitAndInstall();
```

### 扩展方向

- 增量更新
- 回滚机制
- 更新策略配置
- 多服务器支持

---

## 📚 参考资源

- [Electron 官方文档 - autoUpdater](https://www.electronjs.org/docs/latest/api/auto-updater)
- [Semantic Versioning](https://semver.org/)
- [GitHub Releases API](https://docs.github.com/en/rest/releases)
- [HarmonyOS 开发者文档](https://developer.huawei.com/consumer/cn/doc/)

---

## 🔗 相关文章

- [系统托盘功能实现详解](./系统托盘功能实现详解.md)
- [多语言支持实现详解](./多语言支持实现详解.md)
- [多格式文件导出功能说明](./多格式文件导出功能说明.md)

---

<div align="center">

**如果这篇文章对你有帮助，请给我们一个 ⭐️**

Made with ❤️ by HarmonyPC Electron Team

</div>
